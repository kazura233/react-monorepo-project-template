{
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "format": "prettier --write --cache \"**/*.{ts,tsx}\"",
    "lint": "eslint --cache .",
    "clean:modules": "find . -type d -name node_modules -prune -print0 | xargs -0 rm -rf",
    "packages:clean": "pnpm -r --filter=./packages/* run clean",
    "packages:build": "pnpm -r --filter=./packages/* run build",
    "apps:build:development": "pnpm packages:clean && pnpm packages:build && pnpm -r --filter=./apps/* run build:development",
    "apps:build:testing": "pnpm packages:clean && pnpm packages:build && pnpm -r --filter=./apps/* run build:testing",
    "apps:build:production": "pnpm packages:clean && pnpm packages:build && pnpm -r --filter=./apps/* run build:production"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "cross-env": "^10.1.0",
    "eslint": "^10.8.1",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.3",
    "globals": "^17.9.0",
    "prettier": "^3.9.6",
    "typescript-eslint": "^8.66.0",
    "tsx": "^4.23.11"
  }
}